<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head', {title:'Formulario de registro',
    styles:['normalize','new_styles','partials/footer','partials/header']}) %>
</head>
<body class="body-account">
  <%- include('../partials/header') %>
  <main>
    <section class="section">
      <article class="article-container">
        <h1 class="login__title">Registrarse</h1>
        <p class="login__speach">Lorem ipsum dolor, sit amet consectetur adipisicing elit. Laboriosam mollitia beatae nesciunt tenetur voluptatem tempore eligendi. Aspernatur nisi quisquam reiciendis minima inventore eligendi, quam eaque et facilis a nostrum quos.</p>
        <% if (locals.errors && locals.errors.email) { %>
          <div class="form__error">
            <ul>
                <li class="error-msg__text"><%= errors.email.msg %> </li>
              </ul>
          </div>
          <% } %>
        <form action="/users/register" class="form" method="POST">
          <div class="form__layer">
            <!-- <label for="email" class="form__label">Email:</label><br> -->
            <i class="fa-solid fa-user"></i>
            <% if (locals.data && locals.data.nombre) { %>
              <input type="text" name="nombre" class="form__input" placeholder="Nombre" value="<%=locals.data.nombre %>" required>
            <% } else { %>
            <input type="text" name="nombre" class="form__input" placeholder="Nombre" required>
            <% } %>
          </div>
          <div class="form__layer">
            <!-- <label for="email" class="form__label">Email:</label><br> -->
            <i class="fa-solid fa-envelope"></i>
            <% if (locals.data && locals.data.email) { %>
              <input type="email" name="email" class="form__input" placeholder="Email" value="<%= locals.data.email %> "required>
            <% } else { %>
            <input type="email" name="email" class="form__input" placeholder="Email" required>
            <% } %> 
          </div>
          <div class="form__layer">
            <!-- <label for="password" class="form__label">Contraseña:</label><br> -->
            <i class="fa-solid fa-key"></i>
            <input type="password" name="password" class="form__input" required placeholder="Contraseña">
          </div>
          <div class="form__layer">
            <!-- <label for="password" class="form__label">Contraseña:</label><br> -->
            <i class="fa-solid fa-key"></i>
            <input type="password" name="repeat_password" class="form__input" required placeholder="Confirmar contraseña">
          </div>
          <div class="buttons">
            <button type="submit" class="button button--green">Crear cuenta</button>
          </div>
        </form>
        <div class="form__help">
          <p>¿Ya tienes cuenta? <a href="/users/login">¡Click aquí!</a></p>
          <a href="/users/register">¿Olvidaste tu contraseña?</a>
        </div>
      </article>
    </section>
  </main>
</body>
</html>